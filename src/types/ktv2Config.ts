export interface Ktv2Config {
  address: string;
  tokenAddress: string;
  tokenSymbol: string;
  tokenName: string;
  tokenDecimals: number;
  charity: string;
  logoUrl?: string;
  details?: boolean;
} 